(display "MIT Scheme verify: " )
(display (+ 1 2 3))
(newline)
(display "If you see the sum above, MIT Scheme is running.\n")

;; To test scmutils, adjust the load path and load the library per your installation.
;; Example (uncomment and edit path):
;; (load "~/lib/scheme/scmutils/boot.scm")
;; (display "scmutils loaded\n")
